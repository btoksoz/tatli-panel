import React from 'react'
export function Badge({className='', ...props}: any){ return <span className={`inline-flex items-center rounded bg-slate-100 px-2 py-0.5 text-xs font-medium text-slate-700 ${className}`} {...props}/> }